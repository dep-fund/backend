apiVersion: v1
kind: Service
metadata:
  name: depfund-frontend
  namespace: depfund
  labels:
    app: depfund-frontend
spec:
  selector:
    app: depfund-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
