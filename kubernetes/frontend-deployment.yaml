apiVersion: apps/v1
kind: Deployment
metadata:
  name: depfund-frontend
  namespace: depfund
  labels:
    app: depfund-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: depfund-frontend
  template:
    metadata:
      labels:
        app: depfund-frontend
    spec:
      containers:
        - name: frontend
          image: us-central1-docker.pkg.dev/depfund-498022-d7/depfund/frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
